/**
 * List of the main application routes.
 */
export enum routerLinks {
  auth = 'auth',

  login = 'login',

  register = 'register',

  dashboard = 'dashboard',
}

export enum UserRole {
  user = 'USER',
  reviewer = 'REVIEWER'
}
